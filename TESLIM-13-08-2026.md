# Teslim Belgesi ve Talimat — 13.08.2026

**Av. Umut Yücel** · Antalya Barosu, sicil no 6448 · avumutyucelhukuk.com

Bu belge oturumun kapanış teslimidir. Her satır **ölçümle** doğrulandı; ölçüm
yapılamayan yer açıkça öyle yazıldı.

---

## 1. Canlı doğrulama — **21/21 geçti**

`https://avumutyucelhukuk.com` üzerinde 13.08.2026'da ölçüldü:

| # | Kalem | Ölçülen |
|---|---|---|
| 1 | index HTTP | 200 |
| 2 | `<noscript>` künye | 1 |
| 3 | `Person` JSON-LD | var |
| 4 | Yasak şema (`Attorney`/`LocalBusiness`) | 0 |
| 5 | `unpkg` dış origin | 0 |
| 6 | `vendor/` yerel kütüphane | 3 |
| 7 | `defer` / `src` oranı | 22/22 |
| 8 | Açılış perdesi `DOMContentLoaded` içinde | var |
| 9 | `js/varliklar.js` | **619 bayt** (440 KB'lik sürüm geri gelmemiş) |
| 10–15 | Altı ekran dosyasında ücret ibaresi | 0 · 0 · 0 · 0 · 0 · 0 |
| 16 | Ölü `wa.me/message` bağlantısı | 0 |
| 17 | `/api/kod-talebi` içerik türü | `application/json` |
| 18 | Avukat paneli sızıntısı | yok |
| 19 | Worker gönderim anahtarı | `acik` |
| 20 | Günlük gönderim sayacı | 0/40 |
| 21 | Bekleyen kuyruk | 0 |

### Ölçüm sırasında yakalanan kendi hatam

18 numaralı kalem önce **❌** verdi. Doğrulayınca **yanlış alarm** olduğu görüldü:
testim `APP_ENTRY` arıyordu, ama herkese açık site de aynı değişkeni kullanıyor —
`window.APP_ENTRY = 'web'`. Panelinki `'admin'`. Ayırt eden işaret **`AdminShell`**.
Hem test hem `PANEL-GUVENLIK.md`'deki yönerge düzeltildi.

Bu, günün ikinci kendi-hatamı-yakalama vakası; birincisi künye eşitleyicisinin
`TBB Sicil 160505`'i bozacak olmasıydı. İkisi de **kontrol modu sayesinde** yakalandı.

---

## 2. Yapılanlar

### Canlı arıza — müvekkile yalan başarı mesajı ✅ onarıldı
Erişim kodu formu `/api/kod-talebi`'ye POST ediyordu ama uç **hiç yayınlanmamıştı**.
`_redirects` tek sayfa yedeği bilinmeyen her yolu HTTP 200 + HTML ile karşıladığı için
kod `r.ok`'u doğru sanıyor ve müvekkile *"Kodunuz gönderilmiştir"* yazıyordu.
Hiçbir kod gönderilmiyordu. Yanıt doğrulaması eklendi, uç yayına alındı.

### Cevap Robotu — dört yoğunluk bandı ✅
Yarım saatlik aralık **platform tarafından reddedildi** (asgari 1 saat). Kurulan düzen:

| Görev | Türkiye saati | Tur | Bant |
|---|---|---|---|
| Mesai | Hafta içi 09:00–18:00, saatlik | 10 | yoğun |
| Ara Saatler | Her gün 06:00 · 08:00 · 19:00 · 21:00 | 4 | olağan |
| Hafta Sonu | Cmt/Paz 10:00–18:00, 2 saatte bir | 5 | olağan |
| Gece | Her gün 22:00 · 01:00 · 04:00 | 3 | çok az |

Çakışma yok. 🔵 **MAVİ — dosya akıbeti** bandı eklendi: yalnız rehberde `müvekkil`
sınıfına, yalnız kayıtlı veriden, durum uydurmadan (AK m.36 meslek sırrı).

### Künye mimarisi ✅ değişti
Şablon (`{{BURO}}`) yaklaşımı terk edildi — statik sitede ulaşılamaz, üstelik
kendi girdisini yok eden bir derleme adımıydı. Yeni değişmez: künye birden çok
dosyada sabit durabilir, **hepsi `js/buro-bilgi.js` ile aynı olmak zorunda**.
Guard `ZORUNLU=1`, 5/5 temiz.

### Yayın kapısı ✅ üç katman
`pre-commit` (atlanabilir) · `pre-push` (atlanabilir) · **CI** (atlanamaz, `main`'de).

### Karantina ✅
`functions/api/webhook.js` + `wa-webhook.js` → `.KARANTINA`. Üç sebep: çift webhook,
serbest metin gönderimi, sabit yedek doğrulama jetonu.

### Unvan kararı ✅ KALIYOR
Baro yazısı (03.06.2026 · E-91130897-622.03-20599) sicili ve adresi teyit ediyor,
unvan kaydı içermiyor. Kararınız: unvan kalıyor (AK m.43). Hiçbir dosyadan kaldırılmadı.

---

## 3. ⚠️ Sizde bekleyen — öncelik sırasıyla

### 3.1 Doğrulama jetonunu değiştirin — **tek gerçek güvenlik işi**
`.env.example`'daki jeton temizlendi ama **git geçmişinde duruyor**: `07d6aea` commit'i
`WA_VERIFY_TOKEN` ve `VERIFY_TOKEN`'ı 24 karakterlik gerçek hâliyle taşıyor ve o commit
`main`'de, GitHub'da. Dosyadan silmek geçmişten silmez.

**Sıra:** Meta → WhatsApp → Configuration'da yeni Verify Token → Worker `muddy-hat-f441`
değişkeni → Render aracı → Meta'da webhook yeniden doğrulanır.

**Şiddet abartılmamalı:** bu jeton yalnız abonelik el sıkışmasında kullanılır; mesaj
okuma/gönderme yetkisi vermez (onun için `WA_TOKEN` gerekir, o sızmadı) ve depo private.
Acil değil, ertelenmemeli.

**Geçmiş temizliği (`filter-repo`/BFG) önerilmez** — tüm commit karmaları değişir,
worktree dalları ve PR'lar bozulur. Jetonu döndürmek hem güvenli hem ucuz.

### 3.2 Üç ortam değişkeni — kod gönderimi bunlarla açılır
Cloudflare → proje → Settings → Environment variables:

| Değişken | İçerik | Tür |
|---|---|---|
| `KODLAR` | `[{"ad","tel","eposta","kod"}, …]` | **Secret** |
| `WA_PHONE_ID` | `109650188830111` | Plain (gizli değil) |
| `WA_TOKEN` | WhatsApp Cloud API kalıcı jetonu | **Secret** |

> Bu üçünü Claude ve Copilot **giremez**: biri kimlik bilgisi, diğeri 472 kişinin
> adı, telefonu ve kodu. Girilene kadar davranış dürüst kalır.

Doğrulama: `curl -s https://avumutyucelhukuk.com/api/kod-talebi` → `{"ok":true,…}`

### 3.3 Panel kodunu değiştirin
Panel kodu **6448** — yani sitede yayımlanmış baro siciliniz. `PANEL-GUVENLIK.md`
kendi içinde *"sicil numarası gibi tahmin edilebilir bir şey seçmeyiniz"* diyor.
`ui_kits/buro-bilgi.jsx` → `panelKodu`. Panel canlıda **yok**, doğrulandı.

### 3.4 Küçük olanlar
Gmail Gelişmiş Güvenli Tarama · Instagram site bağlantısı ve parola ·
WhatsApp Business "Hakkında" satırı (yalnız telefon uygulamasından).

---

## 4. Copilot'a talimat

**Yapılacaklar, bağımlılık sırasıyla:**

1. **Worker tek dağıtımda iki şablon** — `COPILOT-GOREV-PAKETI-13-08.md` §4:
   `dosya` şablonu (dört sıkı koşul) **ve** `kod` şablonu birlikte. İkisi de aynı
   `SABLONLAR` sözlüğünde; tek yayın yeter.
2. **`ANAHTAR_KELIMELER` listesine ekleme** — `"kod"`, `"erişim kodu"`, `"portal"`,
   `"şifre"`, `"giriş yapamıyorum"`. **Bu yapılmadan "kod" robotu imkânsızdır**:
   şu an "kod" yazan müvekkilin mesajı kenar filtresinde eleniyor, Notion'a hiç
   yazılmıyor. Asıl engel buydu.
3. **E-posta kanalı kararı** — MailChannels ücretsiz Workers servisi kapandı;
   `functions/api/kod-talebi.js` satır 68 bugün 502 döner. Üç seçenek
   `GOREV-3-VE-4-DURUM.md`'de. **Önerim: kanalı kapatın**, WhatsApp'ta bırakın.
4. **`KOD_KV` bağlayın** — şu an bir kod talebi geldiğinde **hiçbir yerde kalıcı iz
   kalmıyor**; yalnız o tarayıcının `localStorage`'ında 20 kayıt duruyor.
5. **`ACK_SOGUMA_SN`** mesai içinde 1800 (30 dk). Tek gevşetme önerim.
6. **44 duruşmanın müvekkil adı** — 9 satır çıkarıldı (5 kesin, 4 teyit bekliyor),
   `umut-yucel-sistem` becerisinde kayıtlı.

**Yapılmayacaklar:**

- Unvanı hiçbir dosyadan kaldırmayın — karar verildi, kalıyor.
- `functions/api/*.KARANTINA` dosyalarını `.js` yapmayın.
- `js/oturum.js`'teki `content-type` doğrulamasını kaldırmayın.
- 1 MB üstü hiçbir HTML'i yayın kaynağı olarak kullanmayın.
- `yayin/panel.html`'i internete koymayın — depoya, `dagitim/`'e, Cloudflare'e.
- Jeton, parola, müvekkil listesi girmeyin.

---

## 5. Yayın kuralları — değişmezler

- Push'tan sonra **en az 3 dakika** bekleyip ölçün. `cf-cache-status: DYNAMIC`
  arıza belirtisi **değildir**.
- Tarayıcıda `.text().length` **karakter**, `wc -c` **bayt** sayar; Türkçe harfler
  2 bayt. Doğrusu `arrayBuffer().byteLength`.
- `js/*.js` derlenmiş React'tir; ücret araması escape biçimlerini de kapsamalı:
  `grep -rEn 'ücretsiz|\\xFCcretsiz|bedelsiz' js/`
- Meşru istisnalar: `articles.js` (CMK m.150 barodan ücretsiz müdafi) ve
  `legal.js` (KVKK m.13) — bunlar mevzuat bilgisidir, ihlal değildir.
- Git kancaları depoda **taşınmaz**; yeni klonda elle kopyalanmalı. Taşınan tek
  koruma CI'dır.

---

## 6. Belge haritası

| Belge | Ne için |
|---|---|
| `COPILOT-YAYIN.md` | Yayın — **tek geçerli kaynak** |
| `COPILOT-GOREV-PAKETI-13-08.md` | Worker şablonları, frenler, dağıtım sınırı |
| `GOREV-3-VE-4-DURUM.md` | Kod otomasyonu ve kanalların gerçek durumu |
| `00-DURDUR-WEBHOOK-GOREVI.md` | Neden o görev paketi uygulanmadı |
| `05_Bellek_Arsivi/KARANTINA-UCLAR/` | İki webhook ucunun karantina gerekçesi |
| `05_Bellek_Arsivi/PANEL/` | Düzeltilmiş panel + güvenlik notu |
| `~/.agents/skills/umut-yucel-sistem/SKILL.md` | Kalıcı sistem hafızası |
